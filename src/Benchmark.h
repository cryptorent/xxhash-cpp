#ifndef BENCHMARK_H
#define BENCHMARK_H


#include <iostream>
#include <vector>
#include <map>
#include <chrono>
#include <limits>
#include "BenchContainer.h"

class Benchmark {
public:
    static void run();
};

#endif //BENCHMARK_H
